const PWAInstallPrompt = () => {
  // Temporarily disabled due to issues.
  // Keep this component dependency-free so it does not affect bundle size.
  return null
}

export default PWAInstallPrompt
